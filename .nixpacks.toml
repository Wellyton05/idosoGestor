[build]
cmd = "composer install --no-dev --optimize-autoloader && npm install && npm run build && php artisan config:cache"

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"

[variables]
PHP_VERSION = "8.1"
NODE_VERSION = "18"